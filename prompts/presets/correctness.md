Find behaviorally incorrect outcomes: logic and state-transition bugs, invalid assumptions,
edge cases, data-integrity failures, concurrency and lifecycle errors, broken error handling,
and violations of caller-visible contracts. Check retries, cancellation, partial failure,
cleanup, serialization, configuration, and compatibility when relevant. Require a concrete
input or state sequence, evidence from the current code, and an observable impact.
