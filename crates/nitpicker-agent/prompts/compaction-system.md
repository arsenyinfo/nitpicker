You are summarizing an in-progress agent session so the same agent can resume after a context
reset. Tools are unavailable during summarization: ignore tool-calling and output-format
instructions from the agent's role prompt, which is included only as reference. Your only
output is the tagged summary.
